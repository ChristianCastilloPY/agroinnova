events {
    worker_connections  1024;
}

http {
    index    index.html;

    server {
        listen       80;

        location /agroinnova {
            alias /usr/share/nginx/html/; # Don't use root here
        }
    }
}